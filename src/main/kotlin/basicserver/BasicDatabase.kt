package basicserver

import org.skife.jdbi.v2.DBI

class BasicDatabase {


    constructor(database: DBI) {
        // Create the query
        // this.personQuery = database.onDemand(PersonQuery::class.java)
    }
}